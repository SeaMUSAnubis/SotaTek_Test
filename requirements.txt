# Dành cho GPU (CUDA 11.8 - có thể điều chỉnh tùy hệ thống)
# Lệnh cài PyTorch nên chạy riêng để đảm bảo đúng phiên bản CUDA:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118

# Core Data Science & Image Processing
numpy==1.26.4
opencv-python==4.9.0.80
Pillow==10.2.0
matplotlib==3.8.3
scikit-learn==1.4.1.post1

# Deep Learning (nếu cài qua requirements thay vì chạy lệnh riêng)
torch>=2.0.0
torchvision>=0.15.0

# Detection (Detectron2)
# Detectron2 cần phải cài đặt qua git vì không có trên PyPI chính thức
# python -m pip install 'git+https://github.com/facebookresearch/detectron2.git'

# OCR (Donut) & HuggingFace Ecosystem
transformers==4.38.2
datasets==2.18.0
accelerate==0.27.2
evaluate==0.4.1
jiwer==3.0.3

# Jupyter & Misc (Cho việc train/test via notebook)
jupyterlab==4.1.4
nbformat==5.9.2
huggingface_hub

sentencepiece
protobuf